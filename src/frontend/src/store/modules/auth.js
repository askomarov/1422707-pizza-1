export default {
  namespaced: true,
  state: {
    auth: [],
  },

  getters: {
    // filteredTasksList(state) { }
  },

  mutations: {
    // setTasksList(state, payload) {    }
  },
  actions: {
    // getTasksList(context, payload) {    }
  },
};
